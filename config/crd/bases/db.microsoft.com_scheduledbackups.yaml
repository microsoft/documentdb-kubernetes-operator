---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: scheduledbackups.db.microsoft.com
spec:
  group: db.microsoft.com
  names:
    kind: ScheduledBackup
    listKind: ScheduledBackupList
    plural: scheduledbackups
    singular: scheduledbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.cluster.name
      name: Cluster
      type: string
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .spec.retentionDays
      name: Retention Days
      type: integer
    name: preview
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScheduledBackupSpec defines the desired state of ScheduledBackup
            properties:
              cluster:
                description: |-
                  Cluster specifies the DocumentDB cluster to backup.
                  The cluster must exist in the same namespace as the ScheduledBackup resource.
                properties:
                  name:
                    description: Name of the referent.
                    type: string
                required:
                - name
                type: object
              retentionDays:
                description: |-
                  RetentionDays specifies how many days the backups should be retained.
                  If not specified, the default retention period from the cluster's backup policy is used.
                type: integer
              schedule:
                description: Schedule defines when backups should be created using
                  cron expression format.
                type: string
            required:
            - cluster
            - schedule
            type: object
          status:
            description: ScheduledBackupStatus defines the observed state of ScheduledBackup
            properties:
              lastScheduledTime:
                description: LastScheduledTime is the time when the last backup was
                  scheduled by this ScheduledBackup.
                format: date-time
                type: string
              nextScheduledTime:
                description: NextScheduledTime is the time when the next backup is
                  scheduled to be created by this ScheduledBackup.
                format: date-time
                type: string
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
