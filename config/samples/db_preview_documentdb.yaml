apiVersion: db.microsoft.com/preview
kind: DocumentDB
metadata:
  labels:
    app.kubernetes.io/name: documentdb-operator
    app.kubernetes.io/managed-by: kustomize
  name: documentdb-sample
spec:
  nodeCount: 1
  instancesPerNode: 1
  resource:
    pvcSize: 1Gi
  documentDBImage: ghcr.io/microsoft/documentdb/documentdb-local:16
  exposeViaService:
    serviceType: ClusterIP
  # Uncomment one TLS mode below
  # tls:
  #   mode: SelfSigned
  # tls:
  #   mode: Provided
  #   provided:
  #     secretName: my-existing-tls-secret
  # tls:
  #   mode: CertManager
  #   certManager:
  #     issuerRef:
  #       name: selfsigned
  #       kind: ClusterIssuer
  #     # dnsNames optional; service DNS names are auto-added
  #     dnsNames:
  #       - extra.example.local
